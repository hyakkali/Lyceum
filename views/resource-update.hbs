<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
    {{> header}}
  </head>
  <body>

    {{> navbar}}

    <div class="wrapper960 center body">
      <div class="col-12">
        <h2 class="text-center">Update {{resource.name}}</h2> <br> <br>
        <form action="/resource-update/{{resource._id}}" method="post">
          <div class="form-group">
            <label>Name</label>
            <input type="text" class="form-control" placeholder="Leave blank to keep original name" id="name" name="name">
          </div> <!-- end div form-group -->
          <br>
          <div class="form-group">
            <label>Description</label>
            <input type="text" class="form-control" placeholder="Leave blank to keep original description" id="description" name="description">
          </div> <!-- end div form-group -->
          <br>
          <div class="form-group">
            <label>Link</label>
            <input type="text" class="form-control" placeholder="Leave blank to keep original link" id="link" name="link">
          </div> <!-- end div form-group -->
          <br>
          <!-- <script>
            function validateForm() {
              var name = document.getElementById('name');
              var description = document.getElementById('description');
              var link = document.getElementById('link');
              var origName = "{{resource.name}}";
              var origDesc = "{{resource.description}}";
              var origLink = "{{resource.link}}";
              if (name.value.trim()==='') {
                name.value = origName;
              }
              if (description.value.trim()==='') {
                description.value = origDesc;
              }
              if (link.value.trim()==='') {
                link.value = origLink;
              }
              return true;
            }
          </script> -->
          <br>
          <button type="submit" class="btn green">Update Resource</button>
        </form>
      </div> <!-- end div col-sm-6 col-sm-offset-3 -->
    </div> <!-- end div container -->

    {{> footer}}

  </body>
</html>
